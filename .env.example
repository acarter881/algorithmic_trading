# Kalshi API credentials
# Generate an RSA key pair and register the public key at https://kalshi.com
AUTOTRADER__KALSHI__API_KEY_ID=your-api-key-id-here
AUTOTRADER__KALSHI__PRIVATE_KEY_PATH=/path/to/private_key.pem

# Environment override (demo or production). Default: demo.
# AUTOTRADER__KALSHI__ENVIRONMENT=demo

# Execution mode override: paper (simulated order placement) or live (real orders).
# Recommended safe default for production API testing: paper.
# AUTOTRADER__KALSHI__EXECUTION_MODE=paper
# AUTOTRADER__KALSHI__EXECUTION_MODE=paper|live

# Discord webhook URL for alerts
# AUTOTRADER__DISCORD__WEBHOOK_URL=https://discord.com/api/webhooks/...
# AUTOTRADER__DISCORD__ENABLED=true

# Database URL override
# For Docker/containerized runs, use an absolute persistent path (for example /data/...)
# and avoid relative sqlite:///... paths, which may write to ephemeral container locations.
# AUTOTRADER__DATABASE__URL=sqlite:////data/autotrader_paper.db

# Logging level override
# AUTOTRADER__LOGGING__LEVEL=DEBUG
